spring.datasource.url=jdbc:postgresql://localhost:5432/biblio
spring.datasource.username=postgres
spring.datasource.password=Asfuck.2004
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=8080
spring.thymeleaf.cache=false

# Désactiver le chargement automatique des données pour éviter les erreurs
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:data.sql

# Logs de débogage pour Spring Security
logging.level.org.springframework.security=DEBUG
logging.level.com.biblio=DEBUG